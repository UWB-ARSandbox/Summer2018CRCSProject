using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ASL.UI.Menus.Vive
{
    /// <summary>
    /// Custom Vive controller radial menu logic handler. Reference in
    /// Vive controllers for easy referral to this class later. Currently
    /// empty and unused.
    /// </summary>
    public class CustomRadialMenu : MonoBehaviour
    {
        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}