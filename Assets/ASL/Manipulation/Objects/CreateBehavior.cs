using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;
using ExitGames.Client.Photon;

namespace ASL.Manipulation.Objects
{
    /// <summary>
    /// Encapsulates different kinds of object creation behavior. Currently 
    /// empty, but should house behaviors similar in concept to CreatePrimitive 
    /// and Instantiate. This class should offer methods that mask the 
    /// complexities of interacting with the PhotonNetwork though.
    /// </summary>
    public class CreateBehavior : MonoBehaviour
    {
        // Insert platform-specific behavior when instantiating objects here
    }
}